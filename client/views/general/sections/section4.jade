.container
  .col-md-6(ng-controller='imagegalleryCtrl')
    .row
      .col-md-4
        .container.slideer
          img.slidee(ng-repeat='photo in photos', ng-swipe-right='showPrev()', ng-swipe-left='showNext()', ng-show='isActive($index)', ng-src='{{photo.src}}')
      .col-md-2
        ul.navi
          li(ng-repeat='photo in photos', ng-class="{'active':isActive($index)}")
            img.gallery(src='{{photo.src}}', alt='{{photo.desc}}', title='{{photo.desc}}', ng-click='showPhoto($index);')  
  .col-md-6(ng-controller='reservenowCtrl')
    .row
      h1 McMacular 2.0
      h3  - Champange collection -
      h2  $ 271
    .row
      .fund(ng-repeat='item in progressData')
        .progress
          .bar(ng-style="{width:{{((item.remaining / item.total) * 100)}} + '%'}") 
            // show calculated percentage if total amount remaining is <= 100%
            span.percent(ng-show='((item.remaining / item.tatal) * 100) <= 100') {{((item.remaining / item.total) * 100) | number:0}} % 
            // show maximum percentage if total amount remaining is > 100%
            span.percent(ng-show='((item.remaining / item.total) * 100) > 100') 100%
        h4.goal
          | {{item.remaining}} Pants remaining out of 300.
    .row
      .accordion__container
        .accordion__tab
          .accordion__tab-titlee(ng-click="openTab('tab three')")
            a: h4(ng-model='activeItemc')
              | {{activeItemc}}
          .accordion__tab-content(ng-show="isOpenTab('tab three')")
            .row(ng-repeat='color in colors',selectable='setActiveItemc(color)',ng-click="closeTab('tab three')")
              a: h4 {{color}}
        .accordion__tab
          .accordion__tab-titlee(ng-click="openTab('tab one')")
            a: h4(ng-model='activeItem')
              | {{activeItem}}
          .accordion__tab-content(ng-show="isOpenTab('tab one')") 
            .row(ng-repeat='fit in fits',selectable='setActiveItem(fit)', ng-click="closeTab('tab one')")
              a: h4 {{fit}}
        .accordion__tab
          .accordion__tab-titlee(ng-click="openTab('tab two')", ng-model='activeItem' )
            a: h4
              | Size {{activesizewaist}}  {{activesizelen}}
          .accordion__tab-content.size(ng-show="isOpenTab('tab two')")
            .row.titlee 
              h5 WAIST
            #boxwaist(ng-repeat='sizew in sizees.waist', selectable='setActiveItemsizew(sizew)') 
              a: h5 {{sizew}}
            .row.titlee 
              h5 LENGTH
            #boxlengthh(ng-repeat='sizel in sizees.lengthh', selectable='setActiveItemsizel(sizel)', ng-click="closeTab('tab one')")
              a: h5 {{sizel}}
        .accordion__tab.res
           button.btn.btn(ng-click='reservenow()'): h5.res Pre-order
           a.btn.btn.danger(ng-show='errorsizefit') {{errorsizefit}}       
        #modalcarthere
          include cart        
        .row.rulerolymp
          .col-md-4
            //- img.img-responsive.olympic(src='../../images/olympic-img.png')
            //- h5 Details            
          .col-md-4.modals
            img.img-responsive.ruler(src='../../images/ruler-img.png')
            a(href='#', data-toggle='modal', data-target='.pop-up-1') Fit Guide
            .modal.fade.pop-up-1(tabindex='-1', role='dialog', aria-labelledby='myLargeModalLabel-1', aria-hidden='true')
              .modal-dialog.modal-md
                .modal-content
                  .modal-header
                    button.close(type='button', data-dismiss='modal', aria-hidden='true') ×
                    h4#myLargeModalLabel-1.modal-title Fit Guide
                  .modal-body
                    img.img-responsive.img-rounded.center-block(src='../../images/fit_guide.jpg', alt='')
                  
                      
            
